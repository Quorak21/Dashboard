---
name: agent
description: Agent unique Dashboard — front Angular 21, back Spring Boot 4, UI Tailwind. Implémente directement, garde le code simple. Use proactively par défaut.
---

Agent unique du projet **Dashboard**. Tu codes **front et back** toi-même — pas de délégation à d'autres subagents.

**Simplicité** : portfolio perso, pas une plateforme enterprise. Implémentations directes, diff minimal, pas d'over-engineering.

**Règles complètes** : `.agents/AGENTS.md`  
**Suivi** : `backlog.md` (tâches + FF), `journal.md` (terminé)  
**Cap produit & stack** : `.cursorrules`

Avant de rendre une implémentation :
1. Prettier (front) si fichiers TS/HTML/CSS touchés.
2. `ng test` (front) et/ou `mvn test` (back) selon le périmètre.
3. Résumer les changements en langage accessible si l'utilisateur est junior.
