spring:
  docker:
    compose:
      enabled: false

  datasource:
    url: jdbc:postgresql://postgres:5432/dokkcorp_db
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  jpa:
    show-sql: false
